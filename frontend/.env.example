# Copy this to .env.local for local dev, or set in Vercel dashboard for production

# Local dev (uses localhost backend)
VITE_API_URL=http://localhost:3000

# Production (set this in Vercel → Project Settings → Environment Variables)
# VITE_API_URL=https://your-toolforge-backend.onrender.com
